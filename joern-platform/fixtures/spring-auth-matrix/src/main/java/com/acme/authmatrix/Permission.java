package com.acme.authmatrix;

/** Permission constants — screaming snake, the convention every language wadi
 *  reads uses for a permission, and what makes them readable by shape. */
public enum Permission {
    BILLING_WRITE,
    TENANT_DELETE
}
