package com.acme.orders;

/** `values()` is synthesized by javac and appears in no source line — the
 *  honest reason for a call to it is `compiler-generated`. */
public enum OrderStatus {
    NEW,
    PAID,
    CANCELLED
}
