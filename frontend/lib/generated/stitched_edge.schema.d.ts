/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Composed edge confidence: min(url recovery, resolution, path match)
 */
export type Confidence = "exact" | "high" | "heuristic" | "none"
export type CreatedAt = string
/**
 * Human-readable resolution trail behind this edge
 */
export type Evidence = string | null
/**
 * Normalized host[:port] — set for EXTERNAL targets only
 */
export type ExternalHost = string | null
export type HttpMethod =
  "GET" | "POST" | "PUT" | "DELETE" | "PATCH" | "HEAD" | "OPTIONS" | "TRACE"
export type Id = string
/**
 * Client mechanism, denormalized for reads
 */
export type Mechanism = string
/**
 * Where this claim came from — orthogonal to confidence, never blended (P7)
 */
export type Provenance =
  | "machine-proven"
  | "config-resolved"
  | "heuristic"
  | "llm-guessed"
  | "human-asserted"
  | "federated"
/**
 * The RemoteCall fact (and ICFG call node) this edge resolves
 */
export type RemoteCallId = string
export type SchemaVersion = string
export type ServiceId = string
export type SnapshotId = string
/**
 * ep_… — set for ANALYZED targets only
 */
export type TargetEndpointId = string | null
/**
 * What a stitched edge lands on (§5.4.2) — the three target kinds that
 * keep partial coverage honest, plus the explicit undetermined fact (P10).
 */
export type TargetKind =
  "analyzed" | "external" | "placeholder" | "undetermined"
/**
 * svc_… (ANALYZED) or ph_… (PLACEHOLDER) — the resolved target service
 */
export type TargetServiceId = string | null
/**
 * The candidate URL this edge resolved; None only when UNDETERMINED
 */
export type Url = string | null

/**
 * One resolved (or explicitly unresolved) cross-service call edge.
 *
 * The envelope ``service_id`` is the CALLER's service id — an edge belongs
 * to the service whose call site it resolves. One ``RemoteCall`` fact can
 * yield N edges (ambiguous resolution, multi-candidate URLs); each is its
 * own row with its own content-derived id (§5.2 over-approximation).
 */
export interface StitchedEdge {
  confidence: Confidence
  created_at?: CreatedAt
  evidence?: Evidence
  external_host?: ExternalHost
  http_verb?: HttpMethod | null
  id: Id
  mechanism: Mechanism
  provenance: Provenance
  remote_call_id: RemoteCallId
  schema_version?: SchemaVersion
  service_id: ServiceId
  snapshot_id: SnapshotId
  target_endpoint_id?: TargetEndpointId
  target_kind: TargetKind
  target_service_id?: TargetServiceId
  url?: Url
}
