{
  "$defs": {
    "Confidence": {
      "description": "Confidence tiers for recovered facts (\u00a75.4). Ordered EXACT > HIGH > HEURISTIC > NONE.",
      "enum": [
        "exact",
        "high",
        "heuristic",
        "none"
      ],
      "title": "Confidence",
      "type": "string"
    },
    "HttpMethod": {
      "enum": [
        "GET",
        "POST",
        "PUT",
        "DELETE",
        "PATCH",
        "HEAD",
        "OPTIONS",
        "TRACE"
      ],
      "title": "HttpMethod",
      "type": "string"
    },
    "Provenance": {
      "description": "Where a claim came from (\u00a75.4). Never blended \u2014 every edge/fact carries one.",
      "enum": [
        "machine-proven",
        "config-resolved",
        "heuristic",
        "llm-guessed",
        "human-asserted",
        "federated"
      ],
      "title": "Provenance",
      "type": "string"
    },
    "TargetKind": {
      "description": "What a stitched edge lands on (\u00a75.4.2) \u2014 the three target kinds that\nkeep partial coverage honest, plus the explicit undetermined fact (P10).",
      "enum": [
        "analyzed",
        "external",
        "placeholder",
        "undetermined"
      ],
      "title": "TargetKind",
      "type": "string"
    }
  },
  "$id": "https://trywadi.com/schemas/1.5.0/stitched_edge.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "One resolved (or explicitly unresolved) cross-service call edge.\n\nThe envelope ``service_id`` is the CALLER's service id \u2014 an edge belongs\nto the service whose call site it resolves. One ``RemoteCall`` fact can\nyield N edges (ambiguous resolution, multi-candidate URLs); each is its\nown row with its own content-derived id (\u00a75.2 over-approximation).",
  "properties": {
    "confidence": {
      "$ref": "#/$defs/Confidence",
      "description": "Composed edge confidence: min(url recovery, resolution, path match)"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "evidence": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Human-readable resolution trail behind this edge",
      "title": "Evidence"
    },
    "external_host": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Normalized host[:port] \u2014 set for EXTERNAL targets only",
      "title": "External Host"
    },
    "http_verb": {
      "anyOf": [
        {
          "$ref": "#/$defs/HttpMethod"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "id": {
      "pattern": "^re_[0-9a-f]{16}$",
      "title": "Id",
      "type": "string"
    },
    "mechanism": {
      "description": "Client mechanism, denormalized for reads",
      "minLength": 1,
      "title": "Mechanism",
      "type": "string"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance",
      "description": "Where this claim came from \u2014 orthogonal to confidence, never blended (P7)"
    },
    "remote_call_id": {
      "description": "The RemoteCall fact (and ICFG call node) this edge resolves",
      "pattern": "^rc_[0-9a-f]{16}$",
      "title": "Remote Call Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.5.0",
      "title": "Schema Version",
      "type": "string"
    },
    "service_id": {
      "title": "Service Id",
      "type": "string"
    },
    "snapshot_id": {
      "title": "Snapshot Id",
      "type": "string"
    },
    "target_endpoint_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "ep_\u2026 \u2014 set for ANALYZED targets only",
      "title": "Target Endpoint Id"
    },
    "target_kind": {
      "$ref": "#/$defs/TargetKind"
    },
    "target_service_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "svc_\u2026 (ANALYZED) or ph_\u2026 (PLACEHOLDER) \u2014 the resolved target service",
      "title": "Target Service Id"
    },
    "url": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The candidate URL this edge resolved; None only when UNDETERMINED",
      "title": "Url"
    }
  },
  "required": [
    "snapshot_id",
    "service_id",
    "id",
    "remote_call_id",
    "mechanism",
    "target_kind",
    "confidence",
    "provenance"
  ],
  "title": "StitchedEdge",
  "type": "object"
}
