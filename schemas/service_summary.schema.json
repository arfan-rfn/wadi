{
  "$defs": {
    "AnalysisCoverage": {
      "additionalProperties": false,
      "description": "Per-service analysis-coverage counts (\u00a75.4.3, schema 1.5.0).\n\nHow much of this service's own production code the endpoint-reachable\nclosure walks. The denominator mirrors the closure's method filters\n(internal, non-synthetic, concrete, service-own sources \u2014 staged\nlibrary code excluded); the numerator is the closure intersected with the\nsame set. Computed in-CPG at export time; absence of this fact on a\nboundary means \"unknown\" (extraction failed / pre-metric snapshot), which\nis never conflated with zero (P10).",
      "properties": {
        "production_methods": {
          "description": "Denominator: the service's own methods",
          "minimum": 0,
          "title": "Production Methods",
          "type": "integer"
        },
        "reachable_methods": {
          "description": "Numerator: methods in >=1 endpoint's reachable closure",
          "minimum": 0,
          "title": "Reachable Methods",
          "type": "integer"
        }
      },
      "required": [
        "production_methods",
        "reachable_methods"
      ],
      "title": "AnalysisCoverage",
      "type": "object"
    },
    "AsyncRoot": {
      "additionalProperties": false,
      "description": "A non-endpoint reachability root (\u00a75.4.2 T4, schema 1.7.0): a method\nthe framework invokes without an HTTP request \u2014 scheduled jobs, event and\nmessage listeners, boot runners, ``@Bean`` factories, framework callbacks.\nThe reachable closure (and therefore coverage, sinks, and stitched edges)\nis rooted at endpoints plus these; a controller-less service is non-empty\nexactly through this list.",
      "properties": {
        "anchor": {
          "$ref": "#/$defs/SourceAnchor"
        },
        "kind": {
          "description": "Registry kind, e.g. 'scheduled' (tags.py)",
          "minLength": 1,
          "title": "Kind",
          "type": "string"
        },
        "method_signature": {
          "description": "Fully-qualified method signature",
          "minLength": 1,
          "title": "Method Signature",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "method_signature",
        "anchor"
      ],
      "title": "AsyncRoot",
      "type": "object"
    },
    "AuthExtractionGap": {
      "additionalProperties": false,
      "description": "One auth-extraction gap family on a service (\u00a75.2.10, schema 1.17.0).\n\nThe independent oracle's finding: what the SOURCE TEXT says the auth layer\nshould have read, minus what it emitted. Deliberately separate from\n``EndpointAuth.evidence`` \u2014 evidence records enforcement wadi *found*,\nwhile this records enforcement it appears to have *missed*, and folding\nthe second into the first would make a miss indistinguishable from a\nclean service.",
      "properties": {
        "code": {
          "$ref": "#/$defs/AuthGapCode"
        },
        "count": {
          "description": "Occurrences across the service's sources",
          "minimum": 1,
          "title": "Count",
          "type": "integer"
        },
        "detail": {
          "anyOf": [
            {
              "maxLength": 500,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What the oracle saw versus what the export carried",
          "title": "Detail"
        },
        "sample_sites": {
          "description": "Up to 5 example sites \u2014 examples, never the exhaustive list",
          "items": {
            "$ref": "#/$defs/SourceAnchor"
          },
          "maxItems": 5,
          "title": "Sample Sites",
          "type": "array"
        }
      },
      "required": [
        "code",
        "count"
      ],
      "title": "AuthExtractionGap",
      "type": "object"
    },
    "AuthGapCode": {
      "description": "\u00a75.2.10 auth-extraction gap codes \u2014 the independent oracle's findings.\n\n``AuthCoverageSection`` counts what the auth layer *emitted*, so it can\nonly see enforcement wadi already read; a construct dropped before\nemission contributes to none of its counters and leaves the endpoint\nlooking cleanly authenticated. That is how 365 train-ticket-aitest\nendpoints published a confident wrong claim while the tracker read zero.\n\nThese codes come from a second, deliberately dumb reading of the SOURCE\nTEXT that shares no code path with the CPG \u2014 so a gap here means \"the file\nsays something the graph did not\", which no emission-derived counter can\nexpress. Never fatal: a gap is a queryable fact about how far to trust the\nauth answer (P10).\n\nAn enum rather than a string registry (\u00a77): pyright catches producer /\nregistry drift in CI, which a runtime validator could only catch on a\nuser's repository.",
      "enum": [
        "unemitted-access-site",
        "unread-security-config",
        "unresolved-scope",
        "reactive-chain"
      ],
      "title": "AuthGapCode",
      "type": "string"
    },
    "CfgAnomaly": {
      "additionalProperties": false,
      "description": "One structural-invariant violation family on a service's CFGs\n(\u00a75.2.8 M2, schema 1.8.0). Never an error: the weird code lives in real\nrepos, and a violated invariant is a queryable fact about how much the\ngraph can be trusted (P10).",
      "properties": {
        "code": {
          "$ref": "#/$defs/CfgAnomalyCode"
        },
        "count": {
          "description": "Occurrences across the service's methods",
          "minimum": 1,
          "title": "Count",
          "type": "integer"
        },
        "sample_sites": {
          "description": "Up to 5 example sites \u2014 examples, never the exhaustive list",
          "items": {
            "$ref": "#/$defs/SourceAnchor"
          },
          "maxItems": 5,
          "title": "Sample Sites",
          "type": "array"
        }
      },
      "required": [
        "code",
        "count"
      ],
      "title": "CfgAnomaly",
      "type": "object"
    },
    "CfgAnomalyCode": {
      "description": "\u00a75.2.8 M2 structural-invariant violation codes.\n\nEvaluated against the RAW exported CFG of every method on every snapshot \u2014\nbefore the assembler's synthetic entry/exit patching, which would make\nreachability invariants vacuously true. Additive changes bump\n``SCHEMA_VERSION`` minor.\n\nAn enum rather than a string registry (\u00a77, recorded 2026-08-05): pyright\ncatches producer/registry drift in CI, which a runtime validator could\nonly catch on a user's repository.",
      "enum": [
        "disconnected-node",
        "branch-arity",
        "unlabeled-arm",
        "loop-no-back-edge",
        "dangling-edge",
        "exit-unreachable"
      ],
      "title": "CfgAnomalyCode",
      "type": "string"
    },
    "ClientLibrary": {
      "description": "Client-library census vocabulary (\u00a75.4.2).\n\nThe worker detects these by deterministic import scan; presence is a fact,\ncall counts are not claimed (an import is not a call \u2014 P10). See\n``MODELLED_CLIENT_LIBRARIES`` for the subset wadi's sink passes model.",
      "enum": [
        "resttemplate",
        "webclient",
        "feign",
        "restclient",
        "http-interface",
        "jdk-httpclient",
        "okhttp",
        "retrofit",
        "apache-httpclient",
        "unirest"
      ],
      "title": "ClientLibrary",
      "type": "string"
    },
    "EndpointCollision": {
      "additionalProperties": false,
      "description": "Two endpoints of one service derived the SAME content-derived id\n(\u00a77, recorded 2026-08-05, schema 1.18.0).\n\nEndpoint ids are ``hash(service, verb, simplified_uri)`` and the store\nupserts on ``(snapshot_id, service_id, id)``, so a collision does not\nmerge \u2014 the second row *replaces* the first and the endpoint is gone. That\nis how three handlers of a real controller vanished with every honesty\nsurface reading clean: the loss happens at the storage key, downstream of\neverything that counts.\n\nRecorded as a fact rather than resolved silently, because the alternatives\nare both worse. Failing the service would cost a whole map to one duplicate\npair; picking a winner quietly is exactly the behaviour that hid the bug.\nA deterministic winner is kept so the snapshot stays reproducible, and the\nlosers are named here with their handlers so the cause is one click away.\n\n**Cause-independent by design.** The 2026-08-05 instance came from URI\ntruncation, but any future defect that makes two URIs equal \u2014 a bad\nnormalizer, an over-eager simplification \u2014 lands here too. Expected empty\nin healthy operation; non-empty means endpoints were dropped.",
      "properties": {
        "dropped_handlers": {
          "description": "Handler signatures whose endpoints were lost to the collision",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Dropped Handlers",
          "type": "array"
        },
        "endpoint_id": {
          "description": "The id both endpoints derived",
          "minLength": 1,
          "title": "Endpoint Id",
          "type": "string"
        },
        "http_method": {
          "minLength": 1,
          "title": "Http Method",
          "type": "string"
        },
        "kept_handler": {
          "description": "Handler signature of the endpoint that was stored",
          "minLength": 1,
          "title": "Kept Handler",
          "type": "string"
        },
        "uri": {
          "description": "The simplified URI they collapsed onto",
          "minLength": 1,
          "title": "Uri",
          "type": "string"
        }
      },
      "required": [
        "endpoint_id",
        "http_method",
        "uri",
        "kept_handler",
        "dropped_handlers"
      ],
      "title": "EndpointCollision",
      "type": "object"
    },
    "GatewayRoute": {
      "additionalProperties": false,
      "description": "One gateway routing rule extracted from config (\u00a75.4.1 phone-book input).\n\nOnly populated on services that ARE gateways (Spring Cloud Gateway routes\nin application.yml). The stitcher re-resolves ``target_uri`` through the\nphone book after applying the prefix rewrite.",
      "properties": {
        "anchor": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceAnchor"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where in config this route is declared (evidence)"
        },
        "path_prefix": {
          "description": "Matched prefix, e.g. '/api/v1/orders/**'",
          "minLength": 1,
          "title": "Path Prefix",
          "type": "string"
        },
        "route_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Config route id, when present",
          "title": "Route Id"
        },
        "strip_prefix": {
          "default": 0,
          "description": "StripPrefix filter segment count",
          "minimum": 0,
          "title": "Strip Prefix",
          "type": "integer"
        },
        "target_uri": {
          "description": "Forward target, e.g. 'lb://ts-order-service' or 'http://orders:8080'",
          "minLength": 1,
          "title": "Target Uri",
          "type": "string"
        }
      },
      "required": [
        "path_prefix",
        "target_uri"
      ],
      "title": "GatewayRoute",
      "type": "object"
    },
    "NetworkIdentity": {
      "additionalProperties": false,
      "description": "How this service is addressed at runtime, as far as statics can see.\n\nRaw config facts extracted by the worker at extraction time (P2/P6 split:\nthe worker extracts, the stitcher resolves). All fields best-effort \u2014 an\nabsent fact is honest, never guessed (P10).",
      "properties": {
        "application_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "spring.application.name (or equivalent declared identity)",
          "title": "Application Name"
        },
        "config_notes": {
          "description": "Machine-readable notes about config parsing for this service (\u00a75.4.2, T3): 'config-profile-merged:<file>' / 'config-profile-doc-merged:<profiles>' (profile config merged over the base) | 'config-profile-merged-all' (active set unknown \u2014 every profile merged, over-approximation) | 'gateway-filter-unmodelled:<name>' / 'gateway-predicate-unmodelled:<name>' (gateway shape perceived but not modelled)",
          "items": {
            "type": "string"
          },
          "title": "Config Notes",
          "type": "array"
        },
        "discovery_names": {
          "description": "Names this service registers under in service discovery (Eureka/Consul)",
          "items": {
            "type": "string"
          },
          "title": "Discovery Names",
          "type": "array"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Network-relevant config keys: flattened application.* allowlist (profiles merged, T3) plus compose environment/env_file entries in their raw env-var spelling \u2014 the stitcher's relaxed-binding lookup bridges ${dotted.keys} to SCREAMING_SNAKE names (\u00a75.4.2)",
          "title": "Env",
          "type": "object"
        },
        "gateway_discovery_locator": {
          "default": false,
          "description": "Spring Cloud Gateway discovery locator: '/{service-name}/**' forwards to that service by its discovery name",
          "title": "Gateway Discovery Locator",
          "type": "boolean"
        },
        "gateway_routes": {
          "description": "Routing rules when this service is a gateway; empty otherwise",
          "items": {
            "$ref": "#/$defs/GatewayRoute"
          },
          "title": "Gateway Routes",
          "type": "array"
        },
        "hostnames": {
          "items": {
            "type": "string"
          },
          "title": "Hostnames",
          "type": "array"
        },
        "ports": {
          "items": {
            "type": "integer"
          },
          "title": "Ports",
          "type": "array"
        },
        "server_port": {
          "anyOf": [
            {
              "maximum": 65535,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Configured listen port (server.port)",
          "title": "Server Port"
        }
      },
      "title": "NetworkIdentity",
      "type": "object"
    },
    "QuarantinedFact": {
      "additionalProperties": false,
      "description": "A diagnostic fact whose vocabulary this build does not recognize\n(\u00a77, recorded 2026-08-05, schema 1.16.0).\n\nNever fatal and never dropped. Diagnostic facts describe how well analysis\nread the code, not the code itself, so an unreadable one must not cost the\nmap \u2014 but silently discarding it would be the exact gap the registries\nexist to prevent (P10, turned on wadi's own pipeline: a self-observation we\ncannot parse is itself a queryable fact).\n\nExpected **empty in all healthy operation**, unlike ``cfg_anomalies``,\nwhich is expected non-zero on real code forever. That difference is why it\nhas its own home rather than sharing one: an always-zero signal folded into\nan always-noisy one stops being a signal. Non-empty on the fixtures or\nbenchmarks fails CI, while a user's run only ever loses the one footnote.",
      "properties": {
        "count": {
          "default": 1,
          "description": "Occurrences of this value",
          "minimum": 1,
          "title": "Count",
          "type": "integer"
        },
        "registry": {
          "description": "Which vocabulary rejected it, e.g. 'CfgAnomalyCode' or 'async-root'",
          "minLength": 1,
          "title": "Registry",
          "type": "string"
        },
        "sample_anchor": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceAnchor"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "One example site, when the rejected fact carried one"
        },
        "service_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Owning service; None on a snapshot-level artifact",
          "title": "Service Id"
        },
        "value": {
          "description": "The raw unrecognized value, verbatim",
          "minLength": 1,
          "title": "Value",
          "type": "string"
        }
      },
      "required": [
        "registry",
        "value"
      ],
      "title": "QuarantinedFact",
      "type": "object"
    },
    "RequestPolicy": {
      "additionalProperties": false,
      "description": "Request-level policy that gates reach without deciding principal (\u00a75.2.10 T6).\n\nCORS, CSRF and rejection handling \u2014 the third category a ``SecurityConfig``\ndeclares. A service-level fact by nature: these are configured once per\nchain and shape who can reach the service at all.\n\nDeliberately NOT an input to ``EndpointAuth``. A CORS policy decides which\nORIGIN may call and CSRF which requests need a token; neither decides which\nPRINCIPAL may, so merging either into ``authenticated`` would answer a\ndifferent question than the one asked. Published so the question becomes\nanswerable at all \u2014 absent facts made present, never facts made wrong.",
      "properties": {
        "anchor": {
          "$ref": "#/$defs/SourceAnchor"
        },
        "detail": {
          "description": "Origins, or the source text of the decision",
          "title": "Detail",
          "type": "string"
        },
        "kind": {
          "description": "Registry kind, e.g. 'cors' (tags.py)",
          "minLength": 1,
          "title": "Kind",
          "type": "string"
        },
        "scope": {
          "description": "Path scope; '{?}' = read but unresolvable",
          "minLength": 1,
          "title": "Scope",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "scope",
        "detail",
        "anchor"
      ],
      "title": "RequestPolicy",
      "type": "object"
    },
    "ServiceKind": {
      "description": "v1: service | library. Reserved for Phase 9 (\u00a77): function, edge-worker, firmware.\n\n``library`` (\u00a75.2.6): an in-repo module other services depend on \u2014 never\nanalyzed as a service (no endpoints, no own CPG); its sources join each\ndependent service's staged parse. Recorded so classification is queryable.",
      "enum": [
        "service",
        "library",
        "function",
        "edge-worker",
        "firmware"
      ],
      "title": "ServiceKind",
      "type": "string"
    },
    "SourceAnchor": {
      "additionalProperties": false,
      "description": "A file + line range in the text that was actually analyzed.\n\nFor preprocessed files (delombok) the anchor refers to the generated\nvariant \u2014 source-on-demand serves that same text, so anchors and served\nsource stay aligned by construction (\u00a75.3).",
      "properties": {
        "end_line": {
          "minimum": 1,
          "title": "End Line",
          "type": "integer"
        },
        "file": {
          "description": "Path relative to the service build root",
          "minLength": 1,
          "title": "File",
          "type": "string"
        },
        "start_line": {
          "minimum": 1,
          "title": "Start Line",
          "type": "integer"
        },
        "variant": {
          "$ref": "#/$defs/SourceVariant",
          "default": "original"
        }
      },
      "required": [
        "file",
        "start_line",
        "end_line"
      ],
      "title": "SourceAnchor",
      "type": "object"
    },
    "SourceVariant": {
      "description": "Whether an anchor refers to original repo text or a generated variant (\u00a75.3).\n\n``GENERATED`` marks preprocessor output (e.g. delombok'ed Java) \u2014 the text\nthat was actually analyzed, served by source-on-demand with a badge.",
      "enum": [
        "original",
        "generated"
      ],
      "title": "SourceVariant",
      "type": "string"
    }
  },
  "$id": "https://trywadi.com/schemas/1.20.0/service_summary.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A service boundary plus counts aggregated at read time.",
  "properties": {
    "analysis_coverage": {
      "anyOf": [
        {
          "$ref": "#/$defs/AnalysisCoverage"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Analysis-coverage counts (\u00a75.4.3). None = fact unavailable (library, extraction failed, or pre-1.5 snapshot) \u2014 never zero"
    },
    "async_roots": {
      "description": "Non-endpoint reachability roots (\u00a75.4.2 T4). Empty also for pre-1.7 snapshots \u2014 absence of the fact, not proof of none",
      "items": {
        "$ref": "#/$defs/AsyncRoot"
      },
      "title": "Async Roots",
      "type": "array"
    },
    "auth_extraction_gaps": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/AuthExtractionGap"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "\u00a75.2.10 independent-oracle findings: auth constructs the source names that the export did not carry. None = never checked (library, extraction failed, pre-1.17 snapshot); [] = checked and clean \u2014 never conflated (P10)",
      "title": "Auth Extraction Gaps"
    },
    "build_root": {
      "description": "Path of the build root relative to the repo root ('.' = root)",
      "minLength": 1,
      "title": "Build Root",
      "type": "string"
    },
    "build_system": {
      "description": "e.g. 'maven', 'gradle'",
      "minLength": 1,
      "title": "Build System",
      "type": "string"
    },
    "cfg_anomalies": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/CfgAnomaly"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "\u00a75.2.8 M2 structural-invariant violations across this service's method CFGs. None = never checked (library, extraction failed, pre-1.8 snapshot); [] = checked and clean \u2014 never conflated (P10)",
      "title": "Cfg Anomalies"
    },
    "client_libraries": {
      "description": "HTTP client libraries detected by import scan (\u00a75.4.2 census, ClientLibrary vocabulary). Presence facts only \u2014 an import is not a call (P10)",
      "items": {
        "$ref": "#/$defs/ClientLibrary"
      },
      "title": "Client Libraries",
      "type": "array"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "endpoint_collisions": {
      "description": "Endpoints of this service that derived the same content-derived id and so could not all be stored (\u00a77). Expected empty; non-empty means endpoints were dropped",
      "items": {
        "$ref": "#/$defs/EndpointCollision"
      },
      "title": "Endpoint Collisions",
      "type": "array"
    },
    "endpoint_count": {
      "default": 0,
      "minimum": 0,
      "title": "Endpoint Count",
      "type": "integer"
    },
    "extraction_error": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Set when this service's CPG extraction failed (\u00a75.2.6 per-service isolation): the error, recorded as a queryable fact (P10). The service then has no endpoints/calls \u2014 absence with a stated cause, never silence",
      "title": "Extraction Error"
    },
    "kind": {
      "$ref": "#/$defs/ServiceKind",
      "default": "service"
    },
    "languages": {
      "description": "e.g. ['java']",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Languages",
      "type": "array"
    },
    "library_roots": {
      "description": "Build roots of in-repo library modules whose sources were staged into this service's parse (\u00a75.2.6 source union); empty for kind=library and for services with no in-repo dependencies",
      "items": {
        "type": "string"
      },
      "title": "Library Roots",
      "type": "array"
    },
    "name": {
      "description": "Display name (e.g. compose/module name)",
      "minLength": 1,
      "title": "Name",
      "type": "string"
    },
    "network": {
      "$ref": "#/$defs/NetworkIdentity"
    },
    "quarantined_facts": {
      "description": "Diagnostic facts whose vocabulary this build does not recognize (\u00a77). Expected empty; non-empty means version drift, never a property of the analyzed code",
      "items": {
        "$ref": "#/$defs/QuarantinedFact"
      },
      "title": "Quarantined Facts",
      "type": "array"
    },
    "repo": {
      "description": "Normalized repo source this service lives in",
      "minLength": 1,
      "title": "Repo",
      "type": "string"
    },
    "request_policies": {
      "description": "CORS/CSRF/rejection-handling policy declared by this service (\u00a75.2.10 T6). Never merged into any endpoint's auth claim \u2014 these decide which ORIGIN or request shape may reach the service, not which principal. Empty also for pre-1.20 snapshots: absence of the fact, not proof of none",
      "items": {
        "$ref": "#/$defs/RequestPolicy"
      },
      "title": "Request Policies",
      "type": "array"
    },
    "schema_version": {
      "default": "1.20.0",
      "title": "Schema Version",
      "type": "string"
    },
    "service_id": {
      "title": "Service Id",
      "type": "string"
    },
    "snapshot_id": {
      "title": "Snapshot Id",
      "type": "string"
    }
  },
  "required": [
    "snapshot_id",
    "service_id",
    "name",
    "repo",
    "build_root",
    "languages",
    "build_system"
  ],
  "title": "ServiceSummary",
  "type": "object"
}
