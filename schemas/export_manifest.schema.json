{
  "$id": "https://trywadi.com/schemas/1.10.0/export_manifest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The last record of the NDJSON export stream, written as ``manifest.json``.\n\nStreaming it LAST is the integrity mechanism (\u00a714): a consumer that never\nsees the manifest knows the stream was truncated. ``artifact_counts`` is\nauthoritative \u2014 a reader can verify it received every record.\n``produced_at`` is the only field allowed to differ between two exports of\nthe same snapshot (the determinism rule).",
  "properties": {
    "artifact_counts": {
      "additionalProperties": {
        "type": "integer"
      },
      "description": "Per-kind record counts, keyed by CONTRACT_MODELS artifact names",
      "title": "Artifact Counts",
      "type": "object"
    },
    "produced_at": {
      "format": "date-time",
      "title": "Produced At",
      "type": "string"
    },
    "schema_version": {
      "default": "1.10.0",
      "title": "Schema Version",
      "type": "string"
    },
    "snapshot_id": {
      "minLength": 1,
      "title": "Snapshot Id",
      "type": "string"
    },
    "system_id": {
      "minLength": 1,
      "title": "System Id",
      "type": "string"
    },
    "wadi_version": {
      "description": "Version of the orchestrator that produced this bundle",
      "minLength": 1,
      "title": "Wadi Version",
      "type": "string"
    }
  },
  "required": [
    "wadi_version",
    "system_id",
    "snapshot_id"
  ],
  "title": "ExportManifest",
  "type": "object"
}
