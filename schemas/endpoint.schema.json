{
  "$defs": {
    "AuthEffect": {
      "description": "What an enforcement point does to a request that reaches it.\n\n``UNKNOWN`` is a first-class answer, not a placeholder: an enforcement we\ncan see but cannot interpret must say so, because the alternative is\nleaving the endpoint reading as unprotected (\u00a75.2.9).",
      "enum": [
        "require-authenticated",
        "require-roles",
        "require-authorities",
        "permit-all",
        "deny-all",
        "unknown"
      ],
      "title": "AuthEffect",
      "type": "string"
    },
    "AuthEvidence": {
      "additionalProperties": false,
      "description": "One enforcement point that gates \u2014 or could gate \u2014 this endpoint (\u00a75.2.9).\n\nEvery claim carries its source, and every construct that *could* gate a\nrequest is recorded here whether or not its effect could be determined.\nThat is what makes the claim rule on :class:`EndpointAuth` safe: a rule we\nfailed to read is present as ``resolution=opaque`` rather than absent.\n\n**Reader migration (pre-1.13.0 artifacts):** records written before the\nenforcement model carry no ``effect``/``resolution``, so they default to\n``unknown``/``resolved`` \u2014 honest about what was recorded (the effect was\nnever stored) while preserving the claim those artifacts were written with.",
      "properties": {
        "active": {
          "default": true,
          "description": "False when present in source but not in effect",
          "title": "Active",
          "type": "boolean"
        },
        "anchor": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceAnchor"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "authorities": {
          "description": "Authorities required, when distinct from roles",
          "items": {
            "type": "string"
          },
          "title": "Authorities",
          "type": "array"
        },
        "detail": {
          "description": "e.g. '@PreAuthorize(\"hasRole('ADMIN')\")'",
          "minLength": 1,
          "title": "Detail",
          "type": "string"
        },
        "effect": {
          "$ref": "#/$defs/AuthEffect",
          "default": "unknown",
          "description": "What this does to a request that reaches it"
        },
        "expression": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Raw access expression / SpEL, preserved verbatim",
          "title": "Expression"
        },
        "http_method": {
          "anyOf": [
            {
              "$ref": "#/$defs/HttpMethod"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Verb restriction; None means the rule applies to every verb"
        },
        "inactive_reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why it is not in effect, e.g. 'disabled in chain' \u2014 required when inactive",
          "title": "Inactive Reason"
        },
        "kind": {
          "$ref": "#/$defs/AuthEvidenceKind"
        },
        "pattern": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path pattern this is scoped to; '{?}' means read but unresolvable",
          "title": "Pattern"
        },
        "resolution": {
          "$ref": "#/$defs/AuthResolution",
          "default": "resolved",
          "description": "How completely it was read"
        },
        "roles": {
          "description": "Roles required, ROLE_ prefix stripped",
          "items": {
            "type": "string"
          },
          "title": "Roles",
          "type": "array"
        }
      },
      "required": [
        "kind",
        "detail"
      ],
      "title": "AuthEvidence",
      "type": "object"
    },
    "AuthEvidenceKind": {
      "description": "What kind of construct enforces (or could enforce) access (\u00a75.2.9).\n\nThe first three are declarative sources; the rest are enforcement points\nthat gate requests without a security-framework rule behind them. All are\nframework-neutral by construction \u2014 a FastAPI ``Security(...)`` dependency\nor an Express middleware maps onto the same vocabulary (goal 9).",
      "enum": [
        "annotation",
        "security-dsl",
        "config",
        "chain-bypass",
        "interceptor",
        "servlet-filter",
        "aspect",
        "in-handler",
        "gateway",
        "authority-model"
      ],
      "title": "AuthEvidenceKind",
      "type": "string"
    },
    "AuthMechanism": {
      "additionalProperties": false,
      "description": "One authentication mechanism configured on the service that serves this endpoint.\n\nA custom filter is only ever promoted past ``CUSTOM_FILTER`` on evidence\ninside the filter itself \u2014 naming a mechanism from a class name would be\nexactly the fabricated security fact \u00a712 forbids.",
      "properties": {
        "active": {
          "default": true,
          "description": "False when present in source but not in effect",
          "title": "Active",
          "type": "boolean"
        },
        "anchor": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceAnchor"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "detail": {
          "description": "Raw source text or filter class name",
          "minLength": 1,
          "title": "Detail",
          "type": "string"
        },
        "inactive_reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why it is not in effect, e.g. 'disabled in chain' \u2014 required when inactive",
          "title": "Inactive Reason"
        },
        "kind": {
          "$ref": "#/$defs/AuthMechanismKind"
        }
      },
      "required": [
        "kind",
        "detail"
      ],
      "title": "AuthMechanism",
      "type": "object"
    },
    "AuthMechanismKind": {
      "description": "How a caller proves identity \u2014 distinct from what it is then allowed to do.",
      "enum": [
        "jwt-bearer",
        "opaque-token",
        "http-basic",
        "form-login",
        "oauth2-login",
        "oauth2-resource-server",
        "saml2",
        "x509",
        "remember-me",
        "custom-filter",
        "stateless-session"
      ],
      "title": "AuthMechanismKind",
      "type": "string"
    },
    "AuthResolution": {
      "description": "How completely an enforcement point was read.\n\n``OPAQUE`` is what withholds an endpoint's claim (\u00a75.2.9): the construct\nwas detected but its effect or scope could not be determined.",
      "enum": [
        "resolved",
        "partial",
        "opaque"
      ],
      "title": "AuthResolution",
      "type": "string"
    },
    "EndpointAuth": {
      "additionalProperties": false,
      "description": "Structured authorization state of an endpoint (\u00a75.2.9).\n\n``authenticated`` is tri-state (P10): True/False are *claims backed by\nfully-read enforcement*; None means honestly unknown \u2014 never defaulted to\nFalse. The distinction between None and False is load-bearing: None says\n\"we did not read everything that guards this\", False says \"we read it all\nand nothing guards this\". Collapsing them is the failure mode this model\nexists to prevent.\n\n``denied`` refines the True case rather than replacing it. ``denyAll()``\nadmits nobody, so \"a request must be authenticated to pass\" is still true\nof it \u2014 but a route no caller can reach is not the same fact as a working\nprotected route, and rendering the two identically tells an auditor a dead\nendpoint is live. It is a separate boolean, not a fourth value of\n``authenticated``, so every existing reader of the tri-state keeps working.",
      "properties": {
        "authenticated": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Authenticated"
        },
        "authorities": {
          "description": "Authorities required, when the rule names authorities rather than roles",
          "items": {
            "type": "string"
          },
          "title": "Authorities",
          "type": "array"
        },
        "denied": {
          "default": false,
          "description": "A read rule denies EVERY caller (denyAll); the endpoint is unreachable",
          "title": "Denied",
          "type": "boolean"
        },
        "evidence": {
          "description": "Every enforcement point in scope \u2014 read or not (\u00a75.2.9)",
          "items": {
            "$ref": "#/$defs/AuthEvidence"
          },
          "title": "Evidence",
          "type": "array"
        },
        "mechanism": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Framework family, e.g. 'spring-security'; see mechanisms[] for how auth works",
          "title": "Mechanism"
        },
        "mechanisms": {
          "description": "How authentication is performed on this endpoint's service",
          "items": {
            "$ref": "#/$defs/AuthMechanism"
          },
          "title": "Mechanisms",
          "type": "array"
        },
        "roles": {
          "items": {
            "type": "string"
          },
          "title": "Roles",
          "type": "array"
        }
      },
      "title": "EndpointAuth",
      "type": "object"
    },
    "EndpointParam": {
      "additionalProperties": false,
      "properties": {
        "location": {
          "$ref": "#/$defs/ParamLocation"
        },
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "required": {
          "default": true,
          "title": "Required",
          "type": "boolean"
        },
        "type_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Type Name"
        }
      },
      "required": [
        "name",
        "location"
      ],
      "title": "EndpointParam",
      "type": "object"
    },
    "EndpointStatus": {
      "additionalProperties": false,
      "description": "One HTTP status a handler's own code names (\u00a75.2.7 T9).",
      "properties": {
        "anchor": {
          "$ref": "#/$defs/SourceAnchor"
        },
        "code": {
          "maximum": 599,
          "minimum": 100,
          "title": "Code",
          "type": "integer"
        },
        "detail": {
          "description": "The source text that named it",
          "title": "Detail",
          "type": "string"
        },
        "origin": {
          "$ref": "#/$defs/StatusOrigin"
        }
      },
      "required": [
        "code",
        "origin",
        "detail",
        "anchor"
      ],
      "title": "EndpointStatus",
      "type": "object"
    },
    "FieldShape": {
      "additionalProperties": false,
      "description": "One serialized field: the WIRE name (Jackson renames applied).",
      "properties": {
        "java_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The Java field name, when it differs from the wire name",
          "title": "Java Name"
        },
        "name": {
          "description": "Serialized name (@JsonProperty applied)",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "shape": {
          "$ref": "#/$defs/TypeShape"
        }
      },
      "required": [
        "name",
        "shape"
      ],
      "title": "FieldShape",
      "type": "object"
    },
    "HttpMethod": {
      "enum": [
        "GET",
        "POST",
        "PUT",
        "DELETE",
        "PATCH",
        "HEAD",
        "OPTIONS",
        "TRACE"
      ],
      "title": "HttpMethod",
      "type": "string"
    },
    "MethodRef": {
      "additionalProperties": false,
      "description": "Reference to a method: stable content-derived id + human-readable signature.",
      "properties": {
        "id": {
          "minLength": 1,
          "pattern": "^m_[0-9a-f]{16}$",
          "title": "Id",
          "type": "string"
        },
        "signature": {
          "description": "Fully-qualified method signature",
          "minLength": 1,
          "title": "Signature",
          "type": "string"
        }
      },
      "required": [
        "id",
        "signature"
      ],
      "title": "MethodRef",
      "type": "object"
    },
    "ParamLocation": {
      "enum": [
        "path",
        "query",
        "body",
        "header"
      ],
      "title": "ParamLocation",
      "type": "string"
    },
    "ShapeKind": {
      "description": "The recovered wire-shape node kinds (\u00a75.2.7). ``unresolved``/``cycle``/\n``truncated`` are honest terminals \u2014 never fabricated fields (P10).",
      "enum": [
        "object",
        "scalar",
        "array",
        "map",
        "cycle",
        "truncated",
        "unresolved",
        "always-null"
      ],
      "title": "ShapeKind",
      "type": "string"
    },
    "ShapeOrigin": {
      "description": "Which evidence the shape was read from (\u00a75.2.7, amended 2026-08-05).\n\n``declared`` is the signature \u2014 the strongest evidence, and the only one\nused until a raw wrapper (`public HttpEntity query(...)`) leaves nothing to\nunwrap. ``return_expression`` means the payload came from what the handler\nreturns, which is still symbolic truth but rests on a weaker premise: one\nreturn path standing for the whole contract. P7 keeps the two legible\nrather than blending them.",
      "enum": [
        "declared",
        "return-expression"
      ],
      "title": "ShapeOrigin",
      "type": "string"
    },
    "SourceAnchor": {
      "additionalProperties": false,
      "description": "A file + line range in the text that was actually analyzed.\n\nFor preprocessed files (delombok) the anchor refers to the generated\nvariant \u2014 source-on-demand serves that same text, so anchors and served\nsource stay aligned by construction (\u00a75.3).",
      "properties": {
        "end_line": {
          "minimum": 1,
          "title": "End Line",
          "type": "integer"
        },
        "file": {
          "description": "Path relative to the service build root",
          "minLength": 1,
          "title": "File",
          "type": "string"
        },
        "start_line": {
          "minimum": 1,
          "title": "Start Line",
          "type": "integer"
        },
        "variant": {
          "$ref": "#/$defs/SourceVariant",
          "default": "original"
        }
      },
      "required": [
        "file",
        "start_line",
        "end_line"
      ],
      "title": "SourceAnchor",
      "type": "object"
    },
    "SourceVariant": {
      "description": "Whether an anchor refers to original repo text or a generated variant (\u00a75.3).\n\n``GENERATED`` marks preprocessor output (e.g. delombok'ed Java) \u2014 the text\nthat was actually analyzed, served by source-on-demand with a badge.",
      "enum": [
        "original",
        "generated"
      ],
      "title": "SourceVariant",
      "type": "string"
    },
    "StatusOrigin": {
      "description": "How a declared status was read (\u00a75.2.7 T9).",
      "enum": [
        "builder",
        "explicit",
        "annotation",
        "default"
      ],
      "title": "StatusOrigin",
      "type": "string"
    },
    "TriggerKind": {
      "description": "v1: http. Reserved (\u00a77): queue, stream, schedule.",
      "enum": [
        "http",
        "queue",
        "stream",
        "schedule"
      ],
      "title": "TriggerKind",
      "type": "string"
    },
    "TypeShape": {
      "additionalProperties": false,
      "description": "A recovered request/response shape (\u00a75.2.7): the wire contract, walked\nfrom in-CPG type structure with honest terminals.",
      "properties": {
        "element": {
          "anyOf": [
            {
              "$ref": "#/$defs/TypeShape"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Element shape for kind=array; value shape for kind=map"
        },
        "fields": {
          "description": "kind=object only; @JsonIgnore omitted",
          "items": {
            "$ref": "#/$defs/FieldShape"
          },
          "title": "Fields",
          "type": "array"
        },
        "kind": {
          "$ref": "#/$defs/ShapeKind"
        },
        "origin": {
          "$ref": "#/$defs/ShapeOrigin",
          "default": "declared",
          "description": "Evidence the shape was read from; nested shapes are always declared"
        },
        "type_name": {
          "description": "Declared type, e.g. 'com.acme.Pet'",
          "minLength": 1,
          "title": "Type Name",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "type_name"
      ],
      "title": "TypeShape",
      "type": "object"
    }
  },
  "$id": "https://trywadi.com/schemas/1.21.0/endpoint.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "One REST endpoint of one service within a snapshot.",
  "properties": {
    "auth": {
      "$ref": "#/$defs/EndpointAuth"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "declared_statuses": {
      "description": "HTTP statuses the handler's own code NAMES (\u00a75.2.7 T9). Named `declared_` because it is not the set this endpoint can answer with: a 500 from an uncaught exception, a 403 from the security layer and a 404 from the dispatcher appear in no handler source. An empty list means nothing was named, never that the endpoint cannot fail (P10)",
      "items": {
        "$ref": "#/$defs/EndpointStatus"
      },
      "title": "Declared Statuses",
      "type": "array"
    },
    "full_uri": {
      "description": "Route as written, e.g. /orders/{orderId}",
      "minLength": 1,
      "title": "Full Uri",
      "type": "string"
    },
    "handler": {
      "$ref": "#/$defs/MethodRef"
    },
    "http_method": {
      "$ref": "#/$defs/HttpMethod"
    },
    "id": {
      "pattern": "^ep_[0-9a-f]{16}$",
      "title": "Id",
      "type": "string"
    },
    "params": {
      "items": {
        "$ref": "#/$defs/EndpointParam"
      },
      "title": "Params",
      "type": "array"
    },
    "request_schema": {
      "anyOf": [
        {
          "$ref": "#/$defs/TypeShape"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Field-level @RequestBody shape (\u00a75.2.7); None = no body or pre-1.6"
    },
    "response_schema": {
      "anyOf": [
        {
          "$ref": "#/$defs/TypeShape"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Field-level response shape, wrappers unwrapped (\u00a75.2.7)"
    },
    "response_type": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Response Type"
    },
    "schema_version": {
      "default": "1.21.0",
      "title": "Schema Version",
      "type": "string"
    },
    "service_id": {
      "title": "Service Id",
      "type": "string"
    },
    "simplified_uri": {
      "description": "Identity form, e.g. /orders/{?}",
      "minLength": 1,
      "title": "Simplified Uri",
      "type": "string"
    },
    "snapshot_id": {
      "title": "Snapshot Id",
      "type": "string"
    },
    "trigger": {
      "$ref": "#/$defs/TriggerKind",
      "default": "http"
    }
  },
  "required": [
    "snapshot_id",
    "service_id",
    "id",
    "http_method",
    "full_uri",
    "simplified_uri",
    "handler"
  ],
  "title": "Endpoint",
  "type": "object"
}
